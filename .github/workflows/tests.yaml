name: run tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: install dependencies and run tests
        env:
          FOLDER_NAME: sandpiper
        run: |
          curl -fsSL https://pixi.sh/install.sh | sh
          ln -s "$HOME/.pixi/bin/pixi" /usr/local/bin -v
          eval "$(pixi shell-hook -e sandpiper --frozen --manifest-path $GITHUB_WORKSPACE/pixi.toml)"
          wget 'https://www.dropbox.com/scl/fi/hcxbpatci79c6j5jjsru2/sandpiper_21_test.duckdb.gz?rlkey=wkpnosp5o2qebzy7ojdmart9e&st=lw2eilku&dl=0' -O backend/db/sandpiper_21_test.duckdb.gz && gunzip backend/db/sandpiper_21_test.duckdb.gz
          cd vue && npm install && cd -
          pytest tests --capture=no -v
