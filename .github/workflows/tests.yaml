name: run tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: install dependencies and run tests
        env:
          FOLDER_NAME: sandpiper
        run: |
          curl -fsSL https://pixi.sh/install.sh | sh
          ln -s "$HOME/.pixi/bin/pixi" /usr/local/bin -v
          eval "$(pixi shell-hook -e sandpiper --frozen --manifest-path $GITHUB_WORKSPACE/pixi.toml)"
          wget 'https://www.dropbox.com/scl/fi/rygjhlkndtrno7rxubfwp/sandpiper_20_test.sqlite3.gz?rlkey=hhbep2u18z7m4gmb76ig90391&st=zvf9vh1o&dl=0' -O backend/db/sandpiper_20_test.sqlite3.gz && gunzip backend/db/sandpiper_20_test.sqlite3.gz
          cd vue && npm install && cd -
          pytest tests --capture=no -v
