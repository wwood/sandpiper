name: run tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: install dependencies and run tests
        env:
          FOLDER_NAME: sandpiper
        run: |
          curl -fsSL https://pixi.sh/install.sh | sh
          ln -s "$HOME/.pixi/bin/pixi" /usr/local/bin -v
          eval "$(pixi shell-hook -e sandpiper --frozen --manifest-path $GITHUB_WORKSPACE/pixi.toml)"
          wget 'https://www.dropbox.com/scl/fi/ephp6uqfd2zh4kvbybvph/sandpiper_38_test.duckdb.gz?rlkey=nni1ubxf0ozfs1njigvqb2f6o&st=ma0cdgka&dl=0' -O backend/db/sandpiper_38_test.duckdb.gz && gunzip backend/db/sandpiper_38_test.duckdb.gz
          cd vue && npm install && cd -
          pytest tests --capture=no -v
