# AGENTS

This repository requires tests to be run after any change. Use:

```
pytest tests --capture=no -v
```

If changes are made, ensure the test command above has been executed before concluding the work.
